# Optional. Wire up the contact form to actually deliver mail.
# See src/app/api/contact/route.ts — without this set, submissions
# are only logged server-side.
RESEND_API_KEY=
CONTACT_TO_EMAIL=hello@axiomsystems.dev
